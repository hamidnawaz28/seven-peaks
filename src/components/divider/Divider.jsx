/**
 * A divider element that renders a horizontal line.
 *
 * @function
 * @name Divider
 * @returns {JSX.Element} The JSX element for the divider.
 *
 * @example
 * // Render a Divider component.
 * <Divider />
 */
const Divider = () => {
  return <div className="divider"></div>;
};

export default Divider;
